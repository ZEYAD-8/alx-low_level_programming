#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
 * _isdigit - Entry point
 * @c: character to classify.
 * Return: 1 if c is uppercase or otherwise 0.
 *
 */

int _isdigit(int c)
{
if isdigit(c)
return (1);
else
return (0);
}
