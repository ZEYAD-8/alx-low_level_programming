#include <stdio.h>
/**
 * main - function that prints the file name
 * @argc: The number of arguments.
 * @argv: The arguments.
 * Return: Always 0.
 */
int main(int argc, char **argv)
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}

